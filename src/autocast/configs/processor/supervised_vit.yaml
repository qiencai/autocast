# One-shot supervised ViT processor config.
# Channel/step values are auto-filled by setup.py from the datamodule stats.
_target_: autocast.processors.supervised_vit.SupervisedViTProcessor
in_channels: auto
out_channels: auto
n_steps_input: auto
n_steps_output: auto
spatial_resolution: auto
global_cond_channels: auto
include_global_cond: true
hidden_dim: 256
num_heads: 8
n_layers: 8
drop_path: 0.1
groups: 8
patch_size: null
